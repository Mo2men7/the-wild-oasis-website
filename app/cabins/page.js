export const metadata = {
  title: "Cabins",
};

export default function Page() {
  //console.log(data); //logging to server so it will be in the terminal

  return (
    <div>
      <h1>Cabins Page</h1>
    </div>
  );
}
